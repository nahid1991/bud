import work from './work.png';

const IconSelector = (imageString) => {
  switch (imageString) {
    case "work":
      return work;
    default:
      return work;
  }
}

export default IconSelector;